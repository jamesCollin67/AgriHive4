<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="320dp"
    android:layout_height="wrap_content"
    android:padding="24dp"
    android:orientation="vertical"
    android:background="@drawable/bg_white_rounded"
    android:gravity="center">

    <!-- Title: Internet Connection in bold red text -->
    <TextView
        android:id="@+id/tvTitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Internet Connection"
        android:textSize="20sp"
        android:textStyle="bold"
        android:textColor="#FF0000"
        android:gravity="center"
        android:paddingBottom="16dp"/>

    <!-- Message in gray text -->
    <TextView
        android:id="@+id/tvMessage"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="No internet connection detected. Please check your network to continue using the app."
        android:textSize="14sp"
        android:textColor="#808080"
        android:gravity="center"
        android:paddingBottom="24dp"
        android:lineSpacingExtra="4dp"/>

    <!-- Buttons container -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center">

        <!-- Try Again button - outlined/bordered -->
        <Button
            android:id="@+id/btnTryAgain"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_marginEnd="8dp"
            android:text="Try Again"
            android:textColor="#E5C75A"
            android:background="@drawable/button_outlined"
            android:stateListAnimator="@null"/>

        <!-- Cancel button - outlined/bordered -->
        <Button
            android:id="@+id/btnCancel"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_marginStart="8dp"
            android:text="Cancel"
            android:textColor="#E5C75A"
            android:background="@drawable/button_outlined"
            android:stateListAnimator="@null"/>

    </LinearLayout>

</LinearLayout>
